@{
    ViewData["Title"] = "Админ-панель";
    Layout = "_Layout"; // Используем твой основной layout
}

@section Styles {
    <style>
        .admin-hero {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            color: white;
            padding: 2rem 0;
            margin-bottom: 2rem;
            border-radius: 0 0 20px 20px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
        }

        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 1.5rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            border-left: 4px solid #3498db;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        }

        .stat-card.users { border-left-color: #3498db; }
        .stat-card.instruments { border-left-color: #27ae60; }
        .stat-card.trades { border-left-color: #e74c3c; }
        .stat-card.active { border-left-color: #f39c12; }

        .stat-number {
            font-size: 2.5rem;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: #7f8c8d;
            font-size: 0.9rem;
        }

        .admin-links {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
        }

        .admin-link {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            text-decoration: none;
            color: #2c3e50;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .admin-link:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
            border-color: #3498db;
            color: #2c3e50;
            text-decoration: none;
        }

        .link-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            display: block;
        }

        .recent-activity {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .activity-item {
            display: flex;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid #e9ecef;
        }

        .activity-item:last-child {
            border-bottom: none;
        }

        .activity-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #3498db;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            margin-right: 1rem;
            flex-shrink: 0;
        }

        .quick-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-top: 2rem;
        }

        .action-btn {
            background: #3498db;
            color: white;
            border: none;
            padding: 1rem;
            border-radius: 10px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .action-btn:hover {
            background: #2980b9;
            transform: translateY(-2px);
        }

        .system-health {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 1.5rem;
            margin-top: 2rem;
        }

        .health-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.8rem 0;
            border-bottom: 1px solid #e9ecef;
        }

        .health-item:last-child {
            border-bottom: none;
        }

        .health-status {
            padding: 0.3rem 0.8rem;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .status-ok { background: #d5f4e6; color: #27ae60; }
        .status-warning { background: #ffeaa7; color: #e17055; }
        .status-error { background: #fadbd8; color: #e74c3c; }

        @@media (max-width: 768px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }

            .admin-links {
                grid-template-columns: 1fr;
            }

            .admin-hero h1 {
                font-size: 2rem;
            }
        }
    </style>
}

<!-- Hero Section -->
<section class="admin-hero">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <h1 class="display-5 fw-bold">⚙️ Админ-панель</h1>
                <p class="lead mb-0">Управление платформой UspeshnyiTrader</p>
            </div>
            <div class="col-lg-4 text-center">
                <div style="font-size: 4rem;">🛠️</div>
            </div>
        </div>
    </div>
</section>

<div class="container">
    <!-- Statistics -->
    <div class="stats-grid">
        <div class="stat-card users">
            <div class="stat-number">@(ViewBag.Stats?.TotalUsers ?? 0)</div>
            <div class="stat-label">👥 Всего пользователей</div>
        </div>
        <div class="stat-card instruments">
            <div class="stat-number">@(ViewBag.Stats?.TotalInstruments ?? 0)</div>
            <div class="stat-label">📊 Торговых инструментов</div>
        </div>
        <div class="stat-card trades">
            <div class="stat-number">@(ViewBag.Stats?.TotalTrades ?? 0)</div>
            <div class="stat-label">💼 Всего сделок</div>
        </div>
        <div class="stat-card active">
            <div class="stat-number">@(ViewBag.Stats?.ActiveTrades ?? 0)</div>
            <div class="stat-label">⚡ Активных сделок</div>
        </div>
    </div>

    <!-- Admin Links -->
    <div class="admin-links">
        <a href="@Url.Action("Users", "Admin")" class="admin-link">
            <span class="link-icon">👥</span>
            <h5>Управление пользователями</h5>
            <p class="text-muted mb-0">Просмотр и редактирование пользователей</p>
        </a>

        <a href="@Url.Action("Content", "Admin")" class="admin-link">
            <span class="link-icon">📊</span>
            <h5>Управление контентом</h5>
            <p class="text-muted mb-0">Инструменты и торговые данные</p>
        </a>

        <a href="@Url.Action("Statistics", "Admin")" class="admin-link">
            <span class="link-icon">📈</span>
            <h5>Статистика</h5>
            <p class="text-muted mb-0">Аналитика и отчеты</p>
        </a>

        <a href="#" class="admin-link">
            <span class="link-icon">⚙️</span>
            <h5>Настройки</h5>
            <p class="text-muted mb-0">Настройки платформы</p>
        </a>
    </div>

    <div class="row">
        <!-- Recent Activity -->
        <div class="col-lg-8">
            <div class="recent-activity">
                <h4 class="mb-4">📋 Последняя активность</h4>

                <div class="activity-item">
                    <div class="activity-icon">👤</div>
                    <div class="flex-grow-1">
                        <strong>Новый пользователь</strong>
                        <div class="text-muted">demofixed зарегистрировался 5 минут назад</div>
                    </div>
                    <small class="text-muted">12:30</small>
                </div>

                <div class="activity-item">
                    <div class="activity-icon">💼</div>
                    <div class="flex-grow-1">
                        <strong>Успешная сделка</strong>
                        <div class="text-muted">EUR/USD закрыта с прибылью $25</div>
                    </div>
                    <small class="text-muted">12:15</small>
                </div>

                <div class="activity-item">
                    <div class="activity-icon">📊</div>
                    <div class="flex-grow-1">
                        <strong>Обновление цены</strong>
                        <div class="text-muted">GBP/USD изменился на +0.32%</div>
                    </div>
                    <small class="text-muted">12:00</small>
                </div>

                <div class="activity-item">
                    <div class="activity-icon">👥</div>
                    <div class="flex-grow-1">
                        <strong>Вход в систему</strong>
                        <div class="text-muted">Администратор авторизовался</div>
                    </div>
                    <small class="text-muted">11:45</small>
                </div>

                <!-- Quick Actions -->
                <div class="quick-actions">
                    <button class="action-btn" onclick="updateAllPrices()">
                        🔄 Обновить цены
                    </button>
                    <button class="action-btn" onclick="backupDatabase()">
                        💾 Бекап данных
                    </button>
                    <button class="action-btn" onclick="clearCache()">
                        🧹 Очистить кэш
                    </button>
                    <button class="action-btn" onclick="sendNotifications()">
                        📢 Уведомления
                    </button>
                </div>
            </div>
        </div>

        <!-- System Health -->
        <div class="col-lg-4">
            <div class="system-health">
                <h4 class="mb-4">❤️ Состояние системы</h4>

                <div class="health-item">
                    <span>База данных</span>
                    <span class="health-status status-ok">OK</span>
                </div>

                <div class="health-item">
                    <span>Торговый движок</span>
                    <span class="health-status status-ok">OK</span>
                </div>

                <div class="health-item">
                    <span>API шлюзы</span>
                    <span class="health-status status-warning">⚠️</span>
                </div>

                <div class="health-item">
                    <span>Память</span>
                    <span class="health-status status-ok">65%</span>
                </div>

                <div class="health-item">
                    <span>Сеть</span>
                    <span class="health-status status-ok">OK</span>
                </div>

                <!-- Server Info -->
                <div class="mt-4 p-3 bg-light rounded">
                    <h6>🖥️ Информация о сервере</h6>
                    <div class="small">
                        <div>Время: <span id="serverTime">@DateTime.Now.ToString("HH:mm:ss")</span></div>
                        <div>Версия: 2.0.1</div>
                        <div>Аптайм: 7д 12ч 30м</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Обновление времени сервера
        function updateServerTime() {
            const now = new Date();
            document.getElementById('serverTime').textContent =
                now.toLocaleTimeString('ru-RU');
        }
        setInterval(updateServerTime, 1000);

        // Быстрые действия
       
        function backupDatabase() {
            alert('💾 Запущен процесс резервного копирования...');
            // В реальном проекте - AJAX запрос
        }

        function clearCache() {
            alert('🧹 Кэш очищен');
            // В реальном проекте - AJAX запрос
        }

        function sendNotifications() {
            alert('📢 Рассылка уведомлений запущена');
            // В реальном проекте - AJAX запрос
        }

        // Анимация появления
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.stat-card, .admin-link');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                card.style.transition = 'all 0.6s ease';

                setTimeout(() => {
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 100);
            });
        });
    </script>
}